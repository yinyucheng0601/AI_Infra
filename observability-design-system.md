# 数据观测工作台设计系统

**版本：v0.1 · 设计规范草案**  
**日期：2026-09-18**  
**参考页面：pangu-layer-stack.html**  
**适用读者：产品设计师、产品经理、前端开发者，以及协助生成界面的 AI。**

这份文档从现有模型训练观测页面中，提取可复用的视觉语言、组件和交互模式。它的目标是让其他数据观测场景保持相同的秩序、信息密度和操作逻辑，同时允许图形根据数据结构变化。

当前规范来自一个持续迭代的原型，尚未经过多页面验证。文中区分三种依据：

| 标记 | 含义 | 使用方式 |
|---|---|---|
| 已确认 | 本次设计迭代中明确确定的方向 | 新场景优先遵守 |
| 实现参考 | 当前页面采用的具体做法或数值 | 根据场景取用，不机械复制 |
| 建议规范 | 为形成系统而提出的整理或补充 | 实施时验证，并在后续版本确认 |

## 1. 这套系统包含什么

可以把设计系统理解为一套“界面怎样长、怎样用、怎样表达数据”的共同约定。

| 层次 | 通俗解释 | 本系统中的例子 |
|---|---|---|
| 设计原则 | 做取舍时遵循什么 | 主要空间留给数据，说明按需展开 |
| 基础样式 | 颜色、文字、间距的统一规则 | 中性灰、白色选中状态、橙红色异常 |
| 组件 | 可以反复使用的小单元 | 分段切换、信息提示、统计项、异常提示 |
| Pattern／交互模式 | 多个组件怎样配合完成任务 | 从总览定位对象，在侧栏检查数据 |
| 数据表达规则 | 图形与数据之间的约定 | 明暗表示幅度，选中不能改变柱高 |

**适用范围：** 模型观测、算子分析、性能诊断、日志与时间线分析等以“定位对象—查看证据”为主的工作台。

**迁移边界：** 2.5D 层叠、49 层模型、Query 降维投影、RMS 指标和具体异常示例属于当前业务场景。新页面可以使用时间线、关系图、表格或二维矩阵，不需要都采用层叠结构。

## 2. 设计原则

以下为已确认的设计方向。

1. **数据占据视觉中心。** 标题、控制项和说明保持紧凑，主要空间用于观察与比较数据。
2. **灰度承载常态，颜色提示异常。** 日常界面使用中性灰；异常使用 `#FD5644`，避免让装饰、普通按钮或常态数据争用这一颜色。
3. **选择状态与数据含义分开。** 白色边框、编号和位置表达选中；数据的数值、柱高和基础映射保持真实。
4. **总览负责定位，详情负责解释。** 两处使用同一份对象数据；详情增加数值、坐标、统计与异常依据。
5. **解释在需要时出现。** 低频说明进入信息提示，规则和排查建议可展开。理解当前结果所必需的信息仍直接可见。
6. **动效服务于识别。** 动作需要帮助用户找到目标、理解层次或跟踪变化；周边对象的响应不能抢过焦点。

## 3. 基础样式

### 3.1 颜色与用途

“语义颜色”指按用途命名，例如“主要文字色”，这样以后调整色值时，不必逐个修改所有组件。

| 用途 | 色值／规则 | 依据 |
|---|---|---|
| 页面背景 | `#111111` | 已确认 |
| 主体表面 | 建议统一为 `#191919`；需要第二层时使用 `#242424` | 建议规范，取自现有灰度范围 |
| 主要文字 | `#E8E8E8` | 实现参考 |
| 次要文字 | `#A0A0A0` | 实现参考 |
| 常规分隔线 | 建议统一为 `#333333` | 建议规范 |
| 选中分段按钮 | 背景 `#FFFFFF`，文字 `#111111` | 已确认 |
| 选中图形轮廓 | `#FFFFFF` | 已确认 |
| 异常强调色 | `#FD5644` | 已确认，覆盖此前的玫红等版本 |
| 常态数据色阶 | 深灰到近白，同一指标使用统一尺度 | 已确认 |

除异常色外，常态界面保持 R＝G＝B 的中性色，不引入绿、蓝或冷暖色偏。避免渐变背景、发光、玻璃质感和厚重阴影；数据图例中的灰度渐变有明确含义，可以使用。

当前热力色阶经过 `#242424 → #505050 → #888888 → #BCBCBC → #EEEEEE`。这些是**实现参考**，当前代码还包含非线性映射，不能把它们直接解释为原始数值的等距刻度。

异常除了颜色，还应有文字、符号或可查看的证据。对于同时选中且异常的对象，保留白色选择轮廓，并在异常数据位置或提示中使用橙红色。

### 3.2 字体与数字

| 角色 | 建议规则 | 说明 |
|---|---|---|
| 字体 | Inter 或系统无衬线字体 | 保持简洁，中文使用系统适配字体 |
| 页面标题 | 20–24px，字重 500 | 当前紧凑页标题约 20px |
| 侧栏标题 | 16–18px，字重 500 | 当前约 17px |
| 正文与控制项 | 12–14px | 核心操作优先保证可读性 |
| 辅助信息 | 11–12px | 避免承载唯一的关键结论 |
| 数值与编号 | 等宽数字；必要时使用等宽字体 | 方便对齐与比较 |

以上字号是建议范围，不是要求所有页面逐像素一致。矩阵里的微小数字仅作为放大后的阅读内容，默认缩放下还应能通过灰度、悬停和统计理解数据。

### 3.3 间距、边框与圆角

- **已确认：** 数据切片轮廓为 1.5px；选择靠颜色和位置突出，保持克制。
- **建议规范：** 普通容器分隔线为 1px，与数据轮廓分工。
- **已确认：** 小控件使用约 4–5px 的轻微圆角；数据主体保留清晰直边。
- **建议规范：** 新组件优先使用 4、8、12、16、24、32px 的间距档位，减少难以辨认的细碎差异。
- **实现参考：** 当前页面左右留白约 24px，总览与侧栏间距约 24px。现有代码还有 7、10、14px 等局部取值，不需要全部升格为规范。

## 4. 页面信息架构

### 4.1 桌面工作台

页面由四个区域组成，保持清晰的阅读顺序。

| 区域 | 常驻内容 | 按需出现的内容 |
|---|---|---|
| 紧凑页头与工具栏 | 页面名称、当前上下文、主要视图切换、必要图例 | 异常入口、场景设置、回放控制 |
| 主可视化区域 | 对象结构、数据分布、选择反馈 | 悬停预览、局部定位提示 |
| 右侧详情面板 | 当前对象名称、详情图、关键统计 | 异常诊断、说明、排查建议 |
| 底部导航区域 | 当前选择、指标柱状选择器 | 当前选中对象的数值 |

侧栏独立滚动，底部导航保持易于访问。固定区域不能遮挡画布、统计内容或滚动末尾。

顶部避免把同一选择拆成两排重复控制。当前页面将训练阶段与观测对象合并为“前向 · 激活”“反向 · 参数梯度”“更新后 · 权重”。这是当前观测快照的命名方式，**不代表权重只在更新阶段存在或被使用**。迁移时只合并有明确对应关系的维度。

不规定所有页面都必须压到同一个像素高度。验收重点是：首次打开时，用户能看到主体数据并开始操作，次要说明不会占去大块空间。

### 4.2 说明放在哪里

| 内容 | 推荐位置 |
|---|---|
| 当前对象、指标、必要单位与选择状态 | 直接可见 |
| 色阶含义与指标解释 | 图例旁信息按钮 |
| 张量位置、形状、行列含义、采样方式 | 详情标题旁信息按钮 |
| 异常简述与关键证据 | 详情中直接可见，位于统计上方 |
| 完整规则与排查建议 | 异常区域内展开查看 |
| 低频场景设置 | 设置菜单或紧凑下拉框 |

信息提示应支持悬停以及点击／键盘访问，不能只依赖鼠标。若移到提示里的信息会改变用户对数值的理解，例如采样和全量的区别，应保留可发现的入口，并在导出数据时携带该信息。

## 5. 可复用组件清单

| 组件 | 作用 | 必须保留的行为与状态 |
|---|---|---|
| 分段切换 | 在互斥视图间切换 | 当前项白底黑字；切换后同步指标、图例与详情 |
| 信息按钮 | 容纳低频解释 | 入口清晰；鼠标与键盘均可访问 |
| 数据图例 | 解释灰度或颜色 | 标明指标与映射含义；需要时提供单位、范围和尺度 |
| 对象编号 | 快速定位对象 | 靠近对象；选中增强；被遮挡时减弱 |
| 详情面板 | 解释当前选中对象 | 与总览选择同步；不会因普通悬停跳到别的对象 |
| 统计项 | 展示可比较数值 | 有名称与统计范围；数值对齐；单位一致 |
| 异常入口 | 从全局进入异常对象 | 有数量或位置；可定位；无异常时不占用此入口 |
| 局部异常诊断 | 解释当前对象的异常 | 简述、证据、规则、建议；没有局部异常时隐藏 |
| 柱状选择器 | 同时导航与比较 | 共同基线与尺度；选择不改变数值或柱高 |
| 回放控制 | 展示过程顺序 | 播放／暂停／重置及进度；说明是在回放还是模拟计算 |

这是首批组件范围，不是通用组件库的全部。搜索、表格、表单、弹窗等尚未在当前页面中充分验证，应按后续场景需要补充。

## 6. 六种核心交互模式

### 6.1 总览—详情联动

用户先从总览寻找模式或异常，再点击对象查看详情。总览和详情必须共享稳定的对象编号、指标和数据来源。

当前左右矩阵使用同一张 64×64 采样矩阵、相同顺序与色标，右侧额外展示数值。如果其他场景需要“总览聚合、详情原值”，必须明确标出统计粒度，并让用户识别对应区域，不能承诺两者纹理完全一致。

### 6.2 悬停预览与点击选中

| 操作 | 图形反馈 | 详情面板 |
|---|---|---|
| 鼠标进入未选中的可见切片 | 该切片明显抬起，邻居轻微响应 | 保持已选对象 |
| 鼠标离开有效图形进入空白 | 预览结束，恢复已选状态 | 保持已选对象 |
| 点击切片 | 确认选择，保留该层抬起 | 更新为该对象 |
| 点击后 | 相邻层回落，邻层与其他普通层恢复标准间距 | 保持新选择 |
| 再悬停当前选中层 | 不重新触发邻层连带效果 | 保持不变 |

实际命中切片才触发预览，空白处不能根据“最近层”自动弹起。触屏不依赖悬停，应通过点击完成选择。

### 6.3 局部展开

展开的目标是让焦点可辨认，同时保留周边结构。当前采用类似 Dock 的效果：焦点抬起明显，邻层只轻微响应，远处对象基本稳定。

点击确认后，相邻层不保持预览时的抬起与局部拉伸。选中层仍保留独立抬起，前后对称留出空间；普通层组内恢复标准间距。这里区分的是**选中层周围的观察空间**与**普通层之间的额外拉伸**。

2.5D 层序保持一致，不为了突出选中对象而随意改变绘制顺序。该模式迁移到时间线时，可以表现为焦点行展开，不必复制切片形状。

### 6.4 异常定位与证据查看

全局入口回答“哪里需要检查”，局部诊断回答“为什么被标记”。用户从异常入口定位对象后，在详情矩阵统计上方看到异常摘要，再按需展开规则与建议。

局部没有触发异常时，隐藏这块诊断内容。此规则不等于所有监控页面都不能显示健康状态；对于缺数据、规则未运行等情况，需要单独说明，不能直接当作正常。

### 6.5 渐进披露

常驻信息负责完成主要任务，信息按钮解释概念，展开项提供深层证据。收起说明的目的是节省空间，不能让用户找不到当前图形的含义。

例如，矩阵形状与坐标范围进入信息提示后，矩阵标题和图例仍应让用户知道当前正在看哪类数据。

### 6.6 数据与导航合一

底部每根柱对应一个对象：柱高表达指标，编号定位对象，点击改变选择。仅为当前选中的柱显示具体数值，减少密集文字。

所有可比较柱共用基线和尺度。最高柱表示该范围内指标最大，不自动表示最重要或最异常。异常提示与选择状态可以叠加，不能修改柱高来制造高亮。

## 7. 状态如何叠加

状态不是互斥的。一个对象可以同时被选中、触发异常，并受到其他对象部分遮挡。

| 状态 | 表达方式 | 边界 |
|---|---|---|
| 常态 | 中性灰、标准位置与间距 | 保持可读性 |
| 悬停 | 焦点预览、轻微邻层影响 | 不替代点击选择，不改变数据 |
| 选中 | 白色轮廓、编号增强、位置反馈 | 控件可白底黑字；数据图不能整体变白 |
| 异常 | `#FD5644`、异常符号与证据 | 颜色不能作为唯一提示 |
| 遮挡 | 被挡住的编号降低亮度 | 不删除异常记录和全局入口 |
| 选中且异常 | 白色选中轮廓与橙红异常标记并存 | 保持两种含义可辨 |
| 数据缺失／不适用 | 明确文字或独立空状态 | 不显示为零，也不推断健康 |

当前编号遮挡判断与变暗属于近似实现，尚需验证不同缩放和重叠场景。新页面应优先保证焦点编号、异常入口和详情文字的可读性。

## 8. 数据表达约定

以下规则与视觉样式同样重要。

1. **说明观察对象。** “某层的代表性张量”不能被标成“整层所有数据”。
2. **说明统计范围。** 采样统计、分块统计、全量统计需要区分；采样未见异常不能证明全量无异常。
3. **保持可比较性。** 同一指标、同一范围使用统一尺度；不同指标不能仅凭亮度直接比较。
4. **说明聚合方式。** RMS、均值、最大值各有不同含义；不能简单平均颜色或错误地平均分块摘要。
5. **保留符号含义。** 当前绝对值灰度显示幅度；正负数值通过详情表达。带符号色标应另行定义。
6. **异常需要规则。** 大数值不自动等于异常，应与历史基线、参考分布、阈值或明确规则关联。
7. **展示过程与重算数据分开。** 回放已有结果时，切层和播放不应重新生成随机矩阵。真正模拟计算时，应说明数据如何随时间变化。
8. **区分相关与因果。** 多层接连异常是观察结果；只有明确的数据依赖或模拟假设支持时，才能描述为传播路径。

设计交付时，每个图至少应回答：它属于哪个对象、哪个指标、哪个时刻；单位和范围是什么；来自采样还是全量；如何映射为图形；异常依据是什么。开发者可以据此定义数据字段，无需设计师提前选择技术框架。

## 9. 文案与异常信息

文案优先使用“对象＋指标＋状态／数值”的具体表达，避免只写“异常”“响应很强”等含义模糊的结论。

| 场景 | 推荐表达方式 |
|---|---|
| 视图切换 | 前向 · 激活 / 反向 · 参数梯度 / 更新后 · 权重 |
| 统计标题 | 整张采样矩阵统计 |
| 异常摘要 | 对象编号＋触发的规则名称 |
| 证据 | 观测值、参考值、差异及统计范围 |
| 排查建议 | 可以继续检查什么，不把假设写成根因 |
| 模拟结果 | 说明为示例或模拟，交代回放与数据变化的关系 |

RMS 可以解释为“描述整体幅度的均方根”。激活幅度大不等于 token 更重要；梯度与权重的 RMS 比值不等于实际参数更新比例。此类容易误读的解释放在相应图例或统计项旁。

## 10. 响应式与可访问性

以下为建议规范，尚不构成已通过可访问性认证的声明。

- 宽屏使用总览与详情分屏；窄屏可上下排列，保留明确的当前选择。
- 固定底部导航应预留内容空间，避免挡住详情、提示和最后一行数据。
- 可操作控件应有可见的键盘焦点；信息按钮可以通过键盘打开和关闭。
- 悬停为辅助能力，点击和键盘应能完成核心选择任务。
- 异常、选中、缺失等状态不能仅依赖颜色。
- 尊重减少动态效果的系统设置；关闭动画后仍能识别状态变化。
- 小字号、弱化后的编号、密集柱图、窄屏固定区域是重点验证对象。

当前原型已有部分键盘导航与减少动效处理，但文档不宣称所有浏览器、屏幕尺寸和辅助技术均已验证。

## 11. 落地与验收

### 11.1 如何交给开发者或 AI

先传递这份规范与参考页面，再说明新场景的数据结构、用户任务和必要操作。实现时按“基础样式—组件—交互模式—业务数据”分开组织，避免把业务示例写死进通用组件。

当前 HTML 经多次迭代，包含相近灰度和重复样式覆盖。提取代码时应合并同义样式、移除过期规则，不能直接把全部现有 CSS 当作设计系统。是否使用某个前端框架，应根据实际项目决定。

### 11.2 每个新页面的检查清单

- [ ] 主体数据在首屏清晰可见，顶部没有重复控制或大段低频说明。
- [ ] 常态颜色为中性灰，异常色统一为 `#FD5644`。
- [ ] 选中控件白底黑字，图形选择状态没有覆盖数据含义。
- [ ] 总览与详情的对象、时刻、指标、尺度和统计粒度一致或有明确说明。
- [ ] 悬停只在有效目标上触发，离开后恢复，点击后没有残留邻层展开。
- [ ] 异常可定位、有依据；无局部异常时诊断区域隐藏；缺失数据不会被当作正常。
- [ ] 柱高与数值一致，切换选择不会改变数据。
- [ ] 信息提示可访问，固定导航不遮挡内容。
- [ ] 动效减弱后仍可操作，触屏与键盘具有核心操作路径。
- [ ] 在真实内容量、不同窗口尺寸下完成视觉与交互验证。

### 11.3 后续扩展顺序

1. 用这份草案制作组件与交互模式展示页，直观看到状态组合。
2. 选择一个不同场景，例如算子时间线，验证哪些规则真正可复用。
3. 根据验证结果修正规范，再抽取代码组件与生成提示词。

v0.1 的完成意味着规则已形成可讨论的文档，不意味着整个设计系统已经完成跨场景验证。

## 附录：当前页面的实现参考

以下内容用于理解参考原型，不作为所有新页面的默认参数。

| 项目 | 当前做法 | 迁移注意事项 |
|---|---|---|
| 详情宽度 | 桌面侧栏约 410px，宽屏部分布局约 460px | 根据详情内容与显示宽度调整 |
| 矩阵 | 左右同一张 64×64 模拟采样矩阵 | 4096 个采样值不是全量张量 |
| 普通层定位 | SVG 坐标每层约横向 +20、纵向 −6 | 是内部绘图单位，不是固定屏幕像素 |
| 焦点抬起 | 最大约 180 SVG 单位 | 应随视觉密度和可用空间验证 |
| 邻层响应 | 最近邻抬起约为焦点的 19%，更远迅速衰减 | 重点是焦点明显、周边克制 |
| 选中后的间距 | 普通层组内恢复标准间距，选中层前后对称留空 | 不保留预览时邻层的额外拉伸 |
| 编号 | 左上角外侧纯数字，遮挡时降低不透明度 | 遮挡检测为近似，需视觉验证 |
| 动效 | 插值收敛；减少动态效果时直接定位 | 当前时间常数不能当作完整动画时长 |
| 异常判定 | 示例使用参考幅度倍数等规则 | 不可直接作为真实训练报警阈值 |
| 传播展示 | 激活模拟传播结果预先确定，播放沿路径选择层 | 播放不重算矩阵；模拟路径不代表真实训练因果 |
| 参数与梯度异常 | 保留各自的独立示例 | 不因增加激活传播演示而删除其他视图能力 |

复用时应保留“视觉秩序、交互语义、数据诚实”这三项约束，并按新任务选择合适的图形。
